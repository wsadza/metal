{{- if .Values.gatekeeper.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "{{- include "gatekeeper.fullname" . }}"
  labels:
    {{- include "gatekeeper.labels" . | nindent 4 }}
  {{- with .Values.gatekeeper.ingress.annotations }}
  annotations:
  {{- tpl (toYaml .) $ | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.gatekeeper.ingress.className }}
  rules:
    {{- if .Values.gatekeeper.ingress.host }}
    - host: {{ .Values.gatekeeper.ingress.host }}
      http:
    {{- else }}
    - http:
    {{- end }}
        paths:
          - path: {{ .Values.gatekeeper.ingress.path }}
            pathType: {{ .Values.gatekeeper.ingress.pathType }} 
            backend:
              service:
                name: {{ include "gatekeeper.fullname" . }} 
                port:
                  number: {{ .Values.gatekeeper.ingress.portNumber }} 
{{- end }}
